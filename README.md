# AppSplitter

A simple Node.js tool to split large files into 50MB chunks for GitHub.

## Installation

```bash
npm install -g simple-splitter-tool
```

## Usage

Split: zip-tool split my-file.zip
Join: zip-tool join my-file.zip
